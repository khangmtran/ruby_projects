# ruby_projects
- Small programs in Ruby.
- leapYearProg: repeatedly takes in a year and determines if it is a leap year or not.
- TrieProg: Implemented a standard trie. An interactive program for using the trie class to search for strings in files.
